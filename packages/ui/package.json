{
  "name": "@nexa/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal shared UI primitives for apps/web. Extended as the design system grows — kept small deliberately in Phase 1.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "dependencies": {
    "clsx": "^2.1.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.6.0"
  }
}
