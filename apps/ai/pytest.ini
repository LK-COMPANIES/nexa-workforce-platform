[pytest]
asyncio_mode = auto
markers =
    live_smoke: makes a real call to the Anthropic API. Excluded by default (see .github/workflows/ci.yml's ai-tests job, which runs `pytest -m "not live_smoke"`) — only the manually-triggered ai-live-smoke-test job selects it explicitly.
